# 物体属性检查工具使用说明

## 功能概述

这个工具用于检查和编辑3D物体的属性信息，包括类别、描述、材质、尺寸、质量和放置位置等。

## 主要功能

### 1. 界面布局
- **左侧**: 显示物体的GIF旋转动画
- **右侧**: 显示和编辑物体属性
  - Category (类别)
  - Description (描述)
  - Material (材质)
  - Dimensions (尺寸)
  - Mass (质量)
  - Placement (放置位置)

### 2. 选择功能
顶部有4个下拉框，可以快速选择不同的模型：
- **Type (类型)**: 如 commercial
- **Subtype (子类型)**: 如 articulated
- **Category (类别)**: 如 basket
- **Model ID (模型ID)**: 如 02f563c8720e209efec34199dd999a53

### 3. 导航功能
- **⬅️ 上一个**: 跳转到上一个模型
- **➡️ 下一个**: 跳转到下一个模型
- **💾 保存**: 保存当前修改

### 4. 未保存修改提醒（弹窗提示）
当你修改了属性后点击"上一个"或"下一个"时，会弹出确认弹窗：
- **💾 保存并继续**: 保存当前修改后跳转
- **❌ 取消**: 关闭弹窗，留在当前页面继续编辑
- **⚠️ 不保存并继续**: 放弃修改直接跳转

## 启动方法

### 基础启动
```bash
cd /root/projects/object_attributes_annotation_tool
python attributes_check_tool.py
```

### 自定义参数启动
```bash
python attributes_check_tool.py \
  --data_file /path/to/your/data.json \
  --base_path /mnt/data/GRScenes-100/instances/renderings \
  --port 7800
```

### 参数说明
- `--data_file`: 数据文件路径 (默认: test.json)
- `--base_path`: GIF文件基础路径 (默认: /mnt/data/GRScenes-100/instances/renderings)
- `--port`: 服务器端口 (默认: 7800)

## 数据格式

### test.json 格式
```json
[
    {
        "commercial-articulated-basket-02f563c8720e209efec34199dd999a53": "```json\n{\n  \"category\": \"basket\",\n  \"description\": \"描述文本\",\n  \"material\": \"plastic\",\n  \"dimensions\": \"0.30 * 0.20 * 0.25\",\n  \"mass\": \"0.5\",\n  \"placement\": \"OnTable\"\n}\n```"
    }
]
```

### GIF路径规则
Key: `commercial-articulated-basket-02f563c8720e209efec34199dd999a53`

路径: `/mnt/data/GRScenes-100/instances/renderings/commercial_objects/articulated/basket/thumbnails/merged_views/02f563c8720e209efec34199dd999a53/02f563c8720e209efec34199dd999a53_original.gif`

规则:
- `commercial` → `commercial_objects` (添加_objects后缀)
- `articulated` → 直接使用
- `basket` → 直接使用
- `02f563c8720e209efec34199dd999a53` → 模型ID

## 工作流程

1. 启动工具后，会自动加载第一个模型
2. 在顶部下拉框中选择要检查的模型
3. 查看左侧GIF动画和右侧属性信息
4. 如需修改，直接在右侧文本框中编辑
5. 点击"保存"按钮保存修改
6. 使用"上一个"/"下一个"按钮浏览其他模型
7. 如有未保存修改，会自动提醒

## 注意事项

1. 修改后务必点击"保存"按钮，否则会丢失修改
2. 切换模型时会自动检测未保存的修改
3. 数据保存在原始JSON文件中，请注意备份
4. GIF文件路径必须存在，否则左侧会显示空白

